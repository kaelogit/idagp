[build]
  command = "npm run build"
  # Required so publish is not the site root (Netlify UI had "." which breaks the Next plugin)
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
